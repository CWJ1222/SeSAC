const math = Number(prompt('수학 점수는?'));
const eng = prompt('영어 점수는?');
const result = (math + eng) / 2;
console.log('🚀 ~ result:', result);

console.log(Number(true), Number(false));
